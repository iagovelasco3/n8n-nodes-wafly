{
  "name": "n8n-nodes-wafly",
  "version": "1.2.1",
  "description": "n8n community node for the Wafly WhatsApp Bridge API — send messages, manage groups and configure webhooks",
  "keywords": [
    "n8n-community-node-package",
    "n8n",
    "wafly",
    "whatsapp",
    "whatsapp-api",
    "messaging"
  ],
  "license": "MIT",
  "homepage": "https://wafly.com.br",
  "author": {
    "name": "Wafly",
    "email": "contato@wafly.com.br"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/iagovelasco3/n8n-nodes-wafly.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "tsc && gulp build:icons",
    "dev": "tsc --watch",
    "format": "prettier nodes credentials --write",
    "lint": "eslint --ext .ts,.json nodes credentials package.json",
    "lintfix": "eslint --ext .ts,.json nodes credentials package.json --fix",
    "prepublishOnly": "npm run build && npm run lint"
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "n8nNodesApiVersion": 1,
    "credentials": [
      "dist/credentials/WaflyApi.credentials.js"
    ],
    "nodes": [
      "dist/nodes/Wafly/Wafly.node.js"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.0",
    "eslint-plugin-n8n-nodes-base": "^1.11.0",
    "gulp": "^4.0.2",
    "n8n-workflow": "^1.0.0",
    "prettier": "^2.7.1",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "n8n-workflow": "*"
  }
}
